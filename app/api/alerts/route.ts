import {NextRequest,NextResponse} from "next/server"; import {getSupabaseAdmin} from "@/lib/supabase";
export async function POST(req:NextRequest){const b=await req.json().catch(()=>null);if(!b?.email||!b?.productId||!Number(b?.targetPrice))return NextResponse.json({ok:false,error:"Invalid input"},{status:400});const s=getSupabaseAdmin();if(!s)return NextResponse.json({ok:false,error:"Database is not configured"},{status:503});const{error}=await s.from("price_alerts").insert({email:b.email,product_id:b.productId,target_price:Number(b.targetPrice),active:true});if(error)return NextResponse.json({ok:false,error:error.message},{status:500});return NextResponse.json({ok:true});}