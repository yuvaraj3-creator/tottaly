import Link from "next/link"; import {categories,products} from "@/lib/products"; import {ProductCard} from "@/components/ProductCard";
export default async function Categories({searchParams}:{searchParams:Promise<{category?:string}>}){const sp=await searchParams;const c=sp.category;const list=c?products.filter(p=>p.category===c):[];return <main className="container py-12"><h1 className="text-4xl font-black">Categories</h1><div className="flex flex-wrap gap-3 mt-6">{categories().map(x=><Link className={`pill ${x===c?"bg-gray-900 text-white":""}`} href={`/categories?category=${encodeURIComponent(x)}`} key={x}>{x}</Link>)}</div>{c&&<><h2 className="text-2xl font-black mt-10 mb-5">{c}</h2><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div></>}</main>}