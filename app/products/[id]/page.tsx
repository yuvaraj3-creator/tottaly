import Link from "next/link"; import {getProduct,lowest} from "@/lib/products"; 
export default async function Product({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=getProduct(id);if(!p)return <main className="container py-20">Product not found.</main>;return <main className="container py-12"><Link href="/products" className="muted">← Products</Link><div className="grid md:grid-cols-2 gap-10 mt-6"><div className="card overflow-hidden"><img src={p.image} className="w-full h-[420px] object-cover" alt={p.title}/></div><div><div className="muted">{p.brand} · {p.category}</div><h1 className="text-4xl font-black mt-2">{p.title}</h1><p className="mt-5">Lowest listed price</p><div className="text-4xl font-black mt-1">₹{lowest(p).toLocaleString()}</div><h2 className="font-black text-xl mt-8 mb-3">Compare stores</h2><div className="grid gap-3">{p.offers.map(([store,price]:any)=><div key={store} className="card p-4 flex items-center justify-between"><div><b>{store}</b><div className="muted text-sm">Partner offer</div></div><div className="flex items-center gap-4"><b>₹{price.toLocaleString()}</b><a className="btn" href={`/api/affiliate/redirect?url=${encodeURIComponent("https://example.com")}`}>Buy Now</a></div></div>)}</div></div></div></main>}