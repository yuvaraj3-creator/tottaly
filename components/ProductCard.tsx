import Link from "next/link"; import {lowest} from "@/lib/products";
export function ProductCard({p}:{p:any}){return <div className="card overflow-hidden hover:shadow-lg transition"><img src={p.image} className="w-full h-48 object-cover" alt={p.title}/><div className="p-4"><div className="text-xs muted">{p.brand} · {p.category}</div><Link href={`/products/${p.id}`} className="font-bold block mt-1">{p.title}</Link><div className="mt-3 flex items-end justify-between"><div><span className="text-xs muted">From</span><div className="text-xl font-black">₹{lowest(p).toLocaleString()}</div></div><Link className="btn text-sm" href={`/products/${p.id}`}>Compare</Link></div></div></div>}